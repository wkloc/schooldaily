#spring-boot:run -Dactiveprofile=dev
#spring-boot:run -Dactiveprofile=prod
spring:
  application:
    name: schooldaily
  profiles:
    active: dev
  cloud:
      config:
        uri: https://localhost:8440
        username: cloudserveruser
        password: cloudserverpassword